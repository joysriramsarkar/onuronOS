[package]
name = "nilrt"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
nix = { version = "0.27", features = ["process", "signal", "mount", "fs", "user", "sched"] }
libc = "0.2"
nilsd = { path = "../nilsd" }
