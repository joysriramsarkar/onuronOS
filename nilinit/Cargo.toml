[package]
name = "nilinit"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
nix = { version = "0.27", features = ["process", "signal", "mount", "fs"] }
libc = "0.2"
nilsd = { path = "../runtime/nilsd" }
