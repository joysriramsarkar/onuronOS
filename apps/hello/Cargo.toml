[package]
name = "hello"
version = "0.1.0"
edition = "2021"

[dependencies]
nilui = { path = "../../runtime/nilui" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
